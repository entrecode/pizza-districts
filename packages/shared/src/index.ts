// Shared types and Zod schemas used by both web and edge surfaces.
// Edge Functions pull from here via relative paths; the web app via @pd/shared.
//
// Phase 1 keeps this empty on purpose — populate as schemas land.

export {};
