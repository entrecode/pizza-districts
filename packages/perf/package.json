{
  "name": "@pd/perf",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test:budget": "playwright test --config=playwright.config.ts",
    "test:lighthouse": "lhci autorun --config=./lighthouserc.cjs",
    "gate": "pnpm test:budget && pnpm test:lighthouse"
  },
  "devDependencies": {
    "@lhci/cli": "^0.14.0",
    "@playwright/test": "^1.49.1",
    "@types/node": "^22.10.5",
    "typescript": "^5.7.3"
  }
}
